---
- name: Ensure dependencies are installed.
  apt:
    name:
      - apt-transport-https
      - gnupg2
    state: present

- name: Add Elasticsearch apt key.
  get_url:
    url: 'https://artifacts.elastic.co/GPG-KEY-elasticsearch'
    dest: '/etc/apt/trusted.gpg.d/elasticsearch.gpg'

- name: Add Kibana repository.
  apt_repository:
    repo: 'deb [signed-by=/etc/apt/trusted.gpg.d/elasticsearch.gpg] https://artifacts.elastic.co/packages/{{ kibana_version }}/apt stable main'
    state: present
    update_cache: true
